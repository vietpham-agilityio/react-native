import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    width: 120,
    alignItems: 'center',
    gap: 8,
  },
  title: {
    marginTop: 8,
  },
  price: {
    marginTop: 4,
  },
});

export default styles;
