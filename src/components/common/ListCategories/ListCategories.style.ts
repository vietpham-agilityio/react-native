import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  scroll: {
    paddingVertical: 2,
  },
  container: {
    flexDirection: 'row',
    gap: 24,
  },
});

export default styles;
